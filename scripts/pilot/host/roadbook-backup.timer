# Nightly at 22:00 UTC = 03:30 IST (the quiet hour the laptop-era backup
# used). Persistent: a missed night runs at next boot.
[Unit]
Description=Nightly Roadbook pilot backup

[Timer]
OnCalendar=*-*-* 22:00:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
